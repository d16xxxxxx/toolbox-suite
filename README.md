# ToolBox Suite (Static)

This repo is now a pure static frontend app.

## Cloudflare Pages setup
- Framework preset: `None`
- Build command: *(leave empty)*
- Build output directory: `static`

## Local preview
```bash
npm run start
```
Then open `http://localhost:4173`.
